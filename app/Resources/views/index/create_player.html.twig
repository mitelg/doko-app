{% extends 'base.html.twig' %}

{% block body %}
    <h1>Create new player</h1>

    {{ form_start(playerForm) }}
    {{ form_widget(playerForm) }}
    {{ form_end(playerForm) }}
    <br>
    <div class="form-group btn-group-lg">
        <a href="{{ path('app_doko_index') }}" class="btn btn-primary">Back to homepage</a>
    </div>
{% endblock %}
